[Unit]
Description=Elastic Pi Display backend
Documentation=https://github.com/jamesagarside/elastic-pi-display
After=network-online.target
Wants=network-online.target

[Service]
Type=exec
User=elastic-display
Group=elastic-display
ExecStart=/opt/elastic-pi-display/venv/bin/elastic-display run
Restart=always
RestartSec=5

# Hardening: the service only needs to read its config and serve HTTP.
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
PrivateTmp=true
ReadOnlyPaths=/etc/elastic-pi-display

[Install]
WantedBy=multi-user.target
